import { View, Text } from "react-native";
import React from "react";
import { Logo, ScrollableView } from "components";

export function SignUpVerificationScreen() {
  return (
    <ScrollableView>
      <Logo mode="reduced" />
    </ScrollableView>
  );
}
